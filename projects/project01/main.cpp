#include <iostream>//no ai used.
#include <vector>
#include <string>//for string functions
#include <fstream>//for file reading and writing. ifstream is for reading files, fstream is for writing to them

int main(){
    //not done. 

    return 0;
}